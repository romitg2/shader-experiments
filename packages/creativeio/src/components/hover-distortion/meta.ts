import type { ComponentInfo } from '../../lib/types'

export const hoverDistortionInfo: ComponentInfo = {
  id: 'hover-distortion',
  name: 'Hover Distortion Card',
  category: 'hover-effects',
  tags: ['webgl', 'hover', 'distortion', 'cursor', 'liquid', 'fluid', 'interactive', 'mouse'],
  description:
    'Cursor-driven liquid displacement over an image, powered by a real lightweight fluid velocity sim (splat, self-advection, pressure projection) so the warp swirls and trails naturally instead of snapping to the cursor. Flat and undistorted at rest.',
}
