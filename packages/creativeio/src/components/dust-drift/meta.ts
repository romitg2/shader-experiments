import type { ComponentInfo } from '../../lib/types'

export const dustDriftInfo: ComponentInfo = {
  id: 'dust-drift',
  name: 'Dust Drift',
  category: 'smoke-fluid',
  tags: ['webgl', 'fluid', 'particles', 'ambient', 'subtle', 'interactive', 'mouse'],
  description:
    'A sparser, gentler tuning of the same GPGPU particle technique as Particle Flow — fewer, smaller, slower motes with a wide soft splat and a long lazy fade, reading as floating dust caught and stirred by a passing current rather than an energetic burst. Fully still at rest.',
}
