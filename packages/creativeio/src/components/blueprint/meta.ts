import type { ComponentInfo } from '../../lib/types'

export const blueprintInfo: ComponentInfo = {
  id: 'blueprint',
  name: 'Blueprint Schematic',
  category: 'smoke-fluid',
  tags: ['webgl', 'fluid', 'blueprint', 'cad', 'schematic', 'interactive', 'mouse'],
  description:
    'The same GPU fluid sim as Fluid Simulation, edge-detected and drawn as white contour lines over a solid blueprint-blue backdrop with a technical grid and tick marks — reads like a hand-drafted engineering schematic instead of rendered smoke. Fully still at rest.',
}
