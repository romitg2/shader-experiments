import type { ComponentInfo } from '../../lib/types'

export const softSpreadInfo: ComponentInfo = {
  id: 'soft-spread',
  name: 'Soft Spread',
  category: 'smoke-fluid',
  tags: ['webgl', 'fluid', 'blur', 'transparent', 'subtle', 'interactive', 'mouse'],
  description:
    'The same GPU fluid sim, heavily blurred into a soft featureless glow and rendered at low peak opacity on a transparent canvas — a barely-there translucent bloom meant to sit over real page content rather than an opaque shape on black. Fully still at rest.',
}
