import type { ComponentInfo } from '../../lib/types'

export const checkerboardTextInfo: ComponentInfo = {
  id: 'checkerboard-text',
  name: 'Checkerboard Text Reveal',
  category: 'text-effects',
  tags: ['3d-text', 'reveal', 'gsap', 'kinetic-typography', 'stagger'],
  description:
    'Kinetic typography reveal: a grid of opaque tiles flips open in a diagonally staggered checkerboard wipe to reveal 3D text underneath.',
}
