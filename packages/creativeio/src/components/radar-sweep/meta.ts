import type { ComponentInfo } from '../../lib/types'

export const radarSweepInfo: ComponentInfo = {
  id: 'radar-sweep',
  name: 'Radar Sweep',
  category: 'smoke-fluid',
  tags: ['webgl', 'fluid', 'radar', 'sonar', 'instrument', 'interactive', 'mouse'],
  description:
    'The same GPU fluid sim, rendered as green phosphor returns on a classic circular radar scope — range rings, a crosshair, and a continuously rotating sweep beam with a fading trail, the way real aviation/maritime radar displays scan for contacts. Fully still at rest.',
}
