import type { ComponentInfo } from '../../lib/types'

export const portraitRevealInfo: ComponentInfo = {
  id: 'portrait-reveal',
  name: 'Portrait Reveal',
  category: 'smoke-fluid',
  tags: ['webgl', 'ascii', 'standalone', 'cheap', 'fbm', 'ambient', 'non-interactive'],
  description:
    'Standalone: a domain-warped FBM turbulence field is rendered entirely as ASCII glyphs from a texture atlas. Purely time-driven, not cursor-reactive - a slow drifting offset keeps the pattern continuously flowing and reorganizing on its own.',
}
